{{/*
OCI Charts - ECR 또는 외부 Helm repo에서 참조하는 Multi-source Applications
*/}}
{{- $ecrBase := printf "oci://%s.dkr.ecr.%s.amazonaws.com/helm" .Values.ecr.accountId .Values.ecr.region -}}
{{- $git := .Values.git -}}
{{- $project := .Values.project -}}
{{- $destination := .Values.destination -}}

{{- range $name, $chart := .Values.ociCharts }}
{{- if $chart.enabled }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $name }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "{{ $chart.syncWave | default "0" }}"
    {{- if $chart.description }}
    argocd.argoproj.io/description: {{ $chart.description | quote }}
    {{- end }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ $project }}
  sources:
    # Chart from ECR OCI or external Helm repo
    {{- if $chart.repoURL }}
    - repoURL: {{ $chart.repoURL }}
      {{- if $chart.chart }}
      chart: {{ $chart.chart }}
      {{- end }}
      targetRevision: {{ $chart.version }}
    {{- else }}
    - repoURL: {{ $ecrBase }}/{{ $chart.chart }}
      chart: {{ $chart.chart }}
      targetRevision: {{ $chart.version }}
    {{- end }}
      {{- if or $chart.valueFiles $chart.skipCrds }}
      helm:
        {{- if $chart.skipCrds }}
        skipCrds: true
        {{- end }}
        {{- if $chart.valueFiles }}
        valueFiles:
          {{- range $chart.valueFiles }}
          - {{ . }}
          {{- end }}
        {{- end }}
      {{- end }}
    # Values from Git
    - repoURL: {{ $git.repoURL }}
      targetRevision: {{ $git.targetRevision }}
      ref: values
  destination:
    server: {{ $destination.server }}
    namespace: {{ $chart.namespace }}
  {{- if $chart.ignoreDifferences }}
  ignoreDifferences:
    {{- range $chart.ignoreDifferences }}
    - group: {{ .group | default "" | quote }}
      kind: {{ .kind }}
      {{- if .name }}
      name: {{ .name }}
      {{- end }}
      {{- if .jsonPointers }}
      jsonPointers:
        {{- range .jsonPointers }}
        - {{ . }}
        {{- end }}
      {{- end }}
      {{- if .jqPathExpressions }}
      jqPathExpressions:
        {{- range .jqPathExpressions }}
        - {{ . }}
        {{- end }}
      {{- end }}
    {{- end }}
  {{- end }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      {{- if $chart.syncOptions }}
      {{- range $chart.syncOptions }}
      - {{ . }}
      {{- end }}
      {{- end }}
{{- end }}
{{- end }}
