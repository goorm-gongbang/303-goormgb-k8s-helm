# AI Service Secrets 설정
# AWS Secrets Manager에서 값을 가져옴

namespaces:
  - staging-ai
  - loadtest-ai
  - monitoring  # Grafana PostgreSQL/ClickHouse datasource용
secretName: ai-service-config

# Secrets Manager 키 prefix
secretManagerPrefix: staging/ai-service
