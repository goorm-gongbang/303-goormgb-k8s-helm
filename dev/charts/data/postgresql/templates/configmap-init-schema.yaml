{{- if .Values.initSchema.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgresql-init-schema
  namespace: {{ .Release.Namespace }}
data:
  init-schema.sql: |
{{ .Files.Get "files/init-schema.sql" | indent 4 }}
{{- end }}
