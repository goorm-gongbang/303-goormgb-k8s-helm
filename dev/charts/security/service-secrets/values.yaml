# 서비스 공용 시크릿 설정
# AWS Secrets Manager에서 값을 가져옴

namespace: dev-webs
secretName: service-config
environment: dev

# Secrets Manager 키 prefix
secretManagerPrefix: dev/services

# 환경별 설정
spring:
  profile: dev
