# auth-guard - Dev 환경
contextPath: "/auth"
replicaCount: 2

image:
  name: "dev/goormgb/auth-guard"
  tag: "f08dadc"
  pullPolicy: Always

imagePullSecrets:
  - name: ecr-pull-secret

# Worker 노드에 배치
nodeSelector:
  kubernetes.io/hostname: mini-might

resources:
  requests:
    cpu: "1"
    memory: 1Gi
  limits:
    cpu: "4"
    memory: 4Gi

# Istio
istio:
  sidecar: true
  gateway:
    enabled: false  # API Gateway 뒤에서 동작

# Service
servicePort: &port 8080
service:
  type: ClusterIP
  port: *port

# Health checks (contextPath가 자동으로 추가됨)
livenessProbe:
  httpGet:
    path: /actuator/health/liveness
    port: *port
  initialDelaySeconds: 60
  periodSeconds: 10
  failureThreshold: 3

readinessProbe:
  httpGet:
    path: /actuator/health/readiness
    port: *port
  initialDelaySeconds: 30
  periodSeconds: 5
  failureThreshold: 3

# Prometheus Metrics (Spring Boot Actuator)
metrics:
  enabled: true
  path: /actuator/prometheus
  interval: 15s

# OpenTelemetry Java Agent
otel:
  enabled: true
  agentVersion: "2.11.0"
  collectorEndpoint: "http://otel-collector.monitoring.svc.cluster.local:4317"
