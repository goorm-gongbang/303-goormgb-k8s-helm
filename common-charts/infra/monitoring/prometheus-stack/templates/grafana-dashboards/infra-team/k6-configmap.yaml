{{- $dashboardFiles := .Files.Glob "files/grafana-dashboards/infra-team-observability/k6/*.json" -}}
{{- $folderName := "DEV 부하 테스트(k6)" -}}
{{- $namespacePattern := "dev-webs" -}}
{{- if and .Values.dashboards .Values.dashboards.infraTeam -}}
{{- $folderName = .Values.dashboards.infraTeam.k6FolderName | default "DEV 부하 테스트(k6)" -}}
{{- end -}}
{{- if and .Values.dashboards .Values.dashboards.devTeam -}}
{{- $namespacePattern = .Values.dashboards.devTeam.namespacePattern | default "dev-webs" -}}
{{- end -}}
{{- if gt (len $dashboardFiles) 0 }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards-infra-team-k6
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/part-of: monitoring
    grafana_dashboard: "1"
  annotations:
    grafana_folder: {{ $folderName | quote }}
data:
{{- range $path, $_ := $dashboardFiles }}
  {{ base $path }}: |-
{{ $.Files.Get $path | replace "__NAMESPACE_PATTERN__" $namespacePattern | nindent 4 }}
{{- end }}
{{- end }}
