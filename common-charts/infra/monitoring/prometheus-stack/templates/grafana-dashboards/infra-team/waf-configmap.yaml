{{- $dashboardFiles := .Files.Glob "files/grafana-dashboards/infra-team-observability/waf/*.json" -}}
{{- $folderName := "DEV WAF 모니터링" -}}
{{- if and .Values.dashboards .Values.dashboards.infraTeam .Values.dashboards.infraTeam.wafFolderName -}}
{{- $folderName = .Values.dashboards.infraTeam.wafFolderName -}}
{{- end -}}
{{- if gt (len $dashboardFiles) 0 }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards-infra-team-waf
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/part-of: monitoring
    grafana_dashboard: "1"
  annotations:
    grafana_folder: {{ $folderName | quote }}
data:
{{- range $path, $_ := $dashboardFiles }}
  {{ base $path }}: |-
{{ $.Files.Get $path | nindent 4 }}
{{- end }}
{{- end }}
