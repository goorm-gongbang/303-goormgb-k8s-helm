{{- if .Values.gateway.grafana.enabled }}
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: grafana-vs
  namespace: {{ .Release.Namespace }}
spec:
  hosts:
    - {{ .Values.gateway.grafana.host | quote }}
  gateways:
    - {{ .Values.gateway.grafana.gatewayRef | default "istio-system/api-gateway" }}
  http:
    - match:
        - uri:
            prefix: /
      retries:
        attempts: 3
        perTryTimeout: 2s
        retryOn: connect-failure,refused-stream,503,reset
      route:
        - destination:
            host: {{ .Values.grafana.fullnameOverride | default "grafana" }}
            port:
              number: 80
{{- end }}
