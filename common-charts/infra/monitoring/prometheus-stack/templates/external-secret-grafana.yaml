{{- if .Values.grafana.eso.enabled }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-admin-credentials
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: grafana-admin-credentials
  data:
    - secretKey: admin-user
      remoteRef:
        key: {{ .Values.grafana.eso.secretManagerKey }}
        property: username
    - secretKey: admin-password
      remoteRef:
        key: {{ .Values.grafana.eso.secretManagerKey }}
        property: password
{{- end }}
{{- if .Values.grafana.eso.oauthSecretManagerKey }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-oauth-credentials
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: grafana-oauth-credentials
  data:
    - secretKey: GF_AUTH_GOOGLE_CLIENT_ID
      remoteRef:
        key: {{ .Values.grafana.eso.oauthSecretManagerKey }}
        property: client_id
    - secretKey: GF_AUTH_GOOGLE_CLIENT_SECRET
      remoteRef:
        key: {{ .Values.grafana.eso.oauthSecretManagerKey }}
        property: client_secret
{{- end }}
