apiVersion: v2
name: prometheus-stack
description: Prometheus + Grafana + AlertManager (kube-prometheus-stack wrapper)
type: application
version: 1.0.0
appVersion: "0.79.0"

dependencies:
  - name: kube-prometheus-stack
    version: "67.4.0"
    repository: https://prometheus-community.github.io/helm-charts
