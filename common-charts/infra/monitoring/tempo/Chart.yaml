# Grafana Tempo
# OCI dependency removed - using direct OCI reference in ArgoCD
# ECR: oci://497012402578.dkr.ecr.ap-northeast-2.amazonaws.com/helm/tempo
apiVersion: v2
name: tempo-extras
description: Grafana Tempo extras (if needed)
type: application
version: 1.0.0
appVersion: "2.6.0"
# NOTE: tempo chart is deployed directly via OCI in ArgoCD
