apiVersion: v1
kind: Service
metadata:
  name: {{ include "istio-console.fullname" . }}
  labels:
    {{- include "istio-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "istio-console.selectorLabels" . | nindent 4 }}
