apiVersion: v2
name: cilium-config
description: Cilium CNI with eBPF (kube-proxy replacement, Hubble observability)
type: application
version: 1.0.0
appVersion: "1.17.3"

dependencies:
  - name: cilium
    version: "1.17.3"
    repository: "https://helm.cilium.io"
    condition: cilium.enabled
